// Default fields
export const DEFAULT_NUM_PLAYERS = 2;
export const DEFAULT_NUM_GAMES = 1;

// Max fields
const MAX_NUM_PLAYERS = 6;
const MAX_NUM_GAMES = 6;

export type VIEWS = "banning" | "drafting" | "selecting" | "results";
